import {useState} from 'react';import {copilot} from '@/lib/api';import {Button} from '@/components/ui/button';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';import {BrainCircuit,Send,ShieldCheck} from 'lucide-react';
export default function Copilot(){const [q,setQ]=useState('Which machine is most likely to fail?'),[r,setR]=useState<any>(),[busy,setBusy]=useState(false);async function ask(){if(!q.trim())return;setBusy(true);try{setR(await copilot(q))}finally{setBusy(false)}}return <div className="mx-auto max-w-5xl space-y-6"><div><h1 className="text-3xl font-black">AETHERFORGE COPILOT</h1><p className="mt-1 text-slate-500">Grounded operational assistant — tools first, no invented factory information.</p></div><div className="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><BrainCircuit className="h-5 w-5 text-violet-600"/>Ask the factory</CardTitle></CardHeader><CardContent><div className="grid gap-3 sm:grid-cols-2">{['Which machine is most likely to fail?','Why is CNC-07 unhealthy?','What caused today\'s downtime?','Which machines consume the most energy?'].map(x=><button key={x} onClick={()=>setQ(x)} className="rounded-xl border bg-slate-50 p-3 text-left text-sm hover:bg-white">{x}</button>)}</div><div className="mt-5 flex gap-2"><input value={q} onChange={e=>setQ(e.target.value)} className="h-11 flex-1 rounded-xl border px-4 outline-none focus:ring-2 focus:ring-blue-200"/><Button onClick={ask} disabled={busy}>{busy?'Thinking…':<><Send className="mr-2 h-4 w-4"/>Ask</>}</Button></div>{r&&<div className="mt-5 rounded-2xl border bg-violet-50/60 p-5"><div className="mb-2 flex items-center justify-between"><div className="font-semibold">Finding</div><span className="rounded-full bg-white px-3 py-1 text-xs text-violet-700">{r.mode}</span></div><p className="text-slate-700">{r.answer}</p><div className="mt-4 text-xs text-slate-500">Sources: {(r.sources||[]).map((s:any)=>s.label).join(' · ')||'Factory tool context'}</div></div>}</CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-emerald-600"/>Grounding policy</CardTitle></CardHeader><CardContent className="space-y-3 text-sm text-slate-600"><p>Every answer is labeled DEMO/SIMULATION until an external LLM is explicitly configured.</p><p>Factory facts come from backend tools. Unknown information is not fabricated.</p><p>Knowledge-grounded answers identify their source document when retrieval occurs.</p></CardContent></Card></div></div>}
