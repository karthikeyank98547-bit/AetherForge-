import {createContext,useContext,useState,ReactNode} from 'react';const C=createContext({open:false,set:(v:boolean)=>{}});export function Dialog({children}:{children:ReactNode}){const [open,set]=useState(false);return <C.Provider value={{open,set}}>{children}</C.Provider>}export function DialogTrigger({children}:{children:ReactNode}){const c=useContext(C);return <span onClick={()=>c.set(true)}>{children}</span>}export function DialogContent({children}:{children:ReactNode}){const c=useContext(C);return c.open?<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/35 p-4"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl">{children}<button onClick={()=>c.set(false)} className="mt-4 text-sm text-slate-500">Close</button></div></div>:null}
